# Vasanthi Creations — production build (copy to .env.production or set in CI)

# Backend API base (no trailing slash) — must match your deployed API + /api/v1
VITE_API_URL=https://vasanthi-premium-creations-285104077941.us-west1.run.app/api/v1

# Razorpay (public key only; safe in frontend bundle)
VITE_RAZORPAY_KEY_ID=your_razorpay_key_id

VITE_APP_NAME=Vasanthi Creations
VITE_APP_DESCRIPTION=Premium Designer Sarees & Custom Blouses

# Production: keep false
VITE_ENABLE_DEV_OTP_BYPASS=false
